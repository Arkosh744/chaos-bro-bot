telegram:
  token: "YOUR_BOT_TOKEN"
  owner_id: 0

claude:
  model: "haiku"
  timeout: 120s

scheduler:
  enabled: false
  min_hour: 9
  max_hour: 22

storage:
  db_path: "trickster.db"

group:
  interject_chance: 10  # percentage 0-100, random interjection chance in groups
