# Web dashboard port (default: 11112)
WEB_PORT=11112

# Anthropic API key for Claude (if not using claude CLI auth)
ANTHROPIC_API_KEY=

# Path to claude CLI binary on host (for Docker)
CLAUDE_BIN=/home/arkosh/.local/bin/claude
